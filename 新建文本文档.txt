一开始没看图 误解了题意
数组声明不用加数据类型
if和elif后面有: for后面也有冒号
二维数组的R[i][j]格式为R[i,j]而不是R[i,j]
函数update(R,i,j)而不是 update(R[i,j])
update py 调整文件结构 让函数在前 主函数在后
visual studio设置tab为4个空格
print要关闭换行符
R = [[]] * 9是一种每个数组的索引都相同的数组,是完全一样的9个数组,这里我们要的显然是9个不同的数组
末尾的索引并不是len(R[i])-1而是8
函数逻辑出现问题
耗时7.00-11.00
11.00-11.28写blog